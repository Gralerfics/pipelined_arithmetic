{
    "name": "adder",
    "hyperparameters": [
        {
            "name": "WIDTH",
            "type": "integer",
            "default": "32"
        },
        {
            "name": "STAGE",
            "type": "integer",
            "default": "${WIDTH}"
        }
    ],
    "architectures": {
        "pipelined_ripple_adder": {
            "includes": [
                {
                    "files": ["pipelined_ripple_adder.vhd"]
                }
            ],
            "stages": "${WIDTH}"
        },
        "vivado_adder": {
            "includes": [
                {
                    "files": ["vivado_adder.vhd"]
                }
            ],
            "stages": "${STAGE}",
            "notice": "Remember to create a adder IP in Vivado named ip_vivado_adder_${WIDTH}_${STAGE} ..."
        }
    }
}