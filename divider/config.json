{
    "name": "divider",
    "hyperparameters": [
        {
            "name": "WIDTH",
            "type": "integer",
            "default": "32"
        },
        // TODO: STAGE, DIVISION PER CLOCK, RESULT FORMAT
        {
            "name": "SIGNED",
            "type": "bit",
            "default": "0"
        }
    ],
    "architectures": {
        "pipelined_comparator": {
            "includes": [
                {
                    "files": ["TODO.vhd"]
                }
            ],
            "stages": "TODO"
        }
    }
}